# Spam Checker(Telegram Bot)
Bot that takes a text (for example from email) and says if it is a spam.
At first you should create your bot in Telegram(@BotFather). Then enter api into bot/API.txt file. After that run model/train_model.py and wait a minute. At the end run /bot_gui.py. 
